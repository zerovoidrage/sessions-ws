# Ingress vs Egress: Важное различие

## ❌ Ingress ≠ Egress

### Ingress (Входящие потоки)
- **Назначение:** Получение медиа потоков **в** LiveKit комнату
- **Примеры:**
  - RTMP поток → LiveKit комната
  - WebRTC поток → LiveKit комната
  - SIP звонок → LiveKit комната
- **Лимит на Scale плане:** Unlimited ✅
- **Для нашей задачи:** ❌ Не подходит (это не то, что нам нужно)

### Egress (Исходящие потоки)
- **Назначение:** Экспорт медиа потоков **из** LiveKit комнаты
- **Примеры:**
  - LiveKit комната → RTMP трансляция
  - LiveKit комната → Запись в файл
  - LiveKit комната → WebSocket (для транскрипции) ⭐ **ЭТО НАМ НУЖНО**
- **Лимит на Scale плане:** Нужно проверить в настройках проекта
- **Для нашей задачи:** ✅ Это то, что нам нужно

## 🔍 Где проверить лимит Egress

1. **LiveKit Cloud Dashboard**
   - Settings → Project
   - Секция "Connection limits"
   - Ищите: **"Concurrent Egress requests"** (не Ingress!)

2. **Что искать:**
   ```
   Connection limits:
   - Concurrent participants: 100
   - Concurrent Egress requests: ??? ← ЭТО НАМ НУЖНО
   - Concurrent Ingress requests: Unlimited ← Это не то
   ```

## ⚠️ Важно

**"Unlimited Ingresses" НЕ решает проблему с Egress!**

- Ingress = входящие потоки (не то, что нужно)
- Egress = исходящие потоки (то, что нужно для транскрипции)
- Это **разные лимиты** в настройках проекта

## ✅ Что нужно проверить

В настройках проекта (Settings → Project) найдите:

```
Concurrent Egress requests: ???
```

Если там написано:
- **2** → Нужен upgrade или гибридный подход
- **Unlimited** → Отлично! Можно использовать Track Egress
- **Другое число** → Нужно учитывать при масштабировании

## 📊 Для транскрипции нужно

**Egress** (не Ingress):
- Track Egress для получения аудио треков
- Room Composite Egress для микширования
- Participant Egress для записи участников

**Ingress** не используется для транскрипции!

