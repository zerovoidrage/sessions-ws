# Настройка RTMP для Render.com

## Проблема

Render.com **не поддерживает кастомные TCP порты** (например, RTMP порт 1935), так как:
- Render предоставляет только HTTP/HTTPS на стандартных портах
- RTMP требует TCP соединения на порт 1935
- Кастомные порты недоступны на Render Free/Starter планах

## Решения

### Вариант 1: ngrok для production (быстрое решение) ⚡

**Подходит для:** быстрого запуска, тестирования, MVP

#### Шаги:

1. **Установите ngrok:**
   ```bash
   brew install ngrok  # macOS
   # или скачайте с https://ngrok.com
   ```

2. **Зарегистрируйтесь на ngrok.com** (бесплатно, нужен для TCP туннелей)

3. **Настройте ngrok:**
   ```bash
   ngrok config add-authtoken YOUR_AUTH_TOKEN
   ```

4. **Запустите ngrok туннель на вашей локальной машине или отдельном сервере:**
   ```bash
   ngrok tcp 1935
   ```

5. **Получите хост и порт из вывода:**
   ```
   Forwarding  tcp://0.tcp.ngrok.io:12345 -> localhost:1935
   ```

6. **Установите переменные окружения на Render:**
   ```env
   RTMP_PORT=12345  # Порт из ngrok (НЕ 1935!)
   RTMP_HOST=0.tcp.ngrok.io  # Хост из ngrok
   ```

7. **Обновите код для использования порта из ngrok:**
   - Код уже использует `RTMP_PORT` из переменных окружения
   - Убедитесь, что используете порт из ngrok, а не 1935

**⚠️ Ограничения ngrok Free:**
- TCP туннели требуют ngrok account
- Хост меняется при перезапуске ngrok (нужно обновлять переменные)
- Ограничения по трафику
- Может быть нестабильно для production

**💡 Решение:** Используйте ngrok с фиксированным доменом (платный план) или автоматически обновляйте переменные через API.

---

### Вариант 2: Отдельный VPS для RTMP (рекомендуется для production) 🚀

**Подходит для:** production, стабильность, масштабируемость

#### Шаги:

1. **Создайте VPS** (например, на DigitalOcean, AWS, Hetzner):
   - Минимальные требования: 1 CPU, 1GB RAM, Ubuntu 22.04
   - Стоимость: ~$6-10/месяц

2. **Установите Node.js и зависимости:**
   ```bash
   curl -fsSL https://deb.nodesource.com/setup_20.x | sudo -E bash -
   sudo apt-get install -y nodejs
   ```

3. **Установите FFmpeg:**
   ```bash
   sudo apt-get update
   sudo apt-get install -y ffmpeg
   ```

4. **Скопируйте RTMP сервер код** на VPS:
   - Можно использовать только `ws/server/rtmp-server.ts` и `ws/server/rtmp-ingest.ts`
   - Или развернуть весь WebSocket сервер на VPS

5. **Настройте firewall:**
   ```bash
   sudo ufw allow 1935/tcp
   sudo ufw enable
   ```

6. **Установите переменные окружения на Render:**
   ```env
   RTMP_HOST=your-vps-ip-or-domain.com
   RTMP_PORT=1935
   ```

7. **Запустите RTMP сервер на VPS:**
   ```bash
   npm run dev:ws
   # или используйте PM2 для production
   pm2 start ws/server/index.ts --name rtmp-server
   ```

**✅ Преимущества:**
- Полный контроль над портами
- Стабильность
- Масштабируемость
- Нет ограничений ngrok

---

### Вариант 3: Альтернативный подход - без RTMP сервера

Если RTMP сервер сложно настроить, можно использовать **Track Egress с WebSocket** (старый подход):

1. **Не используйте Room Composite Egress**
2. **Используйте Track Egress** (уже реализовано в `livekit-egress-transcriber.ts`)
3. **Fallback автоматически сработает** если Room Composite не работает

**Настройка:**
```env
# Не устанавливайте RTMP_HOST и RTMP_PORT
# Система автоматически переключится на Track Egress
```

**⚠️ Недостатки:**
- N Egress сессий вместо 1
- Нет микширования на стороне LiveKit
- Сложнее для diarization

---

### Вариант 4: Локальная разработка

Для локальной разработки используйте `localhost`:

```env
RTMP_HOST=localhost
RTMP_PORT=1935
```

**Но:** LiveKit Egress из облака не сможет подключиться к localhost, поэтому:

1. **Используйте ngrok для локальной разработки:**
   ```bash
   ngrok tcp 1935
   # Используйте полученный хост в .env.local
   ```

2. **Или тестируйте только на production** (Render + ngrok)

---

## Рекомендация

**Для MVP/тестирования:**
- Используйте **ngrok** (Вариант 1)
- Быстро, бесплатно (с ограничениями)

**Для production:**
- Используйте **отдельный VPS** (Вариант 2)
- Стабильно, масштабируемо, ~$6-10/месяц

**Для локальной разработки:**
- Используйте **ngrok** для туннелирования localhost

---

## Проверка работы

После настройки проверьте:

1. **RTMP сервер запущен:**
   ```bash
   # На Render или VPS
   curl http://localhost:8000/api/rtmp/status
   ```

2. **Порт доступен:**
   ```bash
   # Проверьте, что порт открыт
   telnet RTMP_HOST RTMP_PORT
   ```

3. **Логи WebSocket сервера:**
   ```
   [RTMPServer] ✅ RTMP server started on port 1935
   ```

4. **Логи при запуске транскрипции:**
   ```
   [RoomCompositeTranscriber] Egress streaming to: rtmp://RTMP_HOST:RTMP_PORT/live/SESSION_SLUG
   ```

---

## Troubleshooting

### ngrok хост меняется при перезапуске

**Проблема:** При перезапуске ngrok получает новый хост.

**Решение:**
1. Используйте ngrok с фиксированным доменом (платный план)
2. Или автоматически обновляйте переменные через ngrok API
3. Или используйте отдельный VPS

### RTMP поток не подключается

**Проверьте:**
1. `RTMP_HOST` и `RTMP_PORT` правильно установлены
2. Порт открыт в firewall (для VPS)
3. RTMP сервер запущен и слушает порт
4. LiveKit Egress может подключиться к RTMP URL

### FFmpeg не может подключиться к RTMP

**Проблема:** FFmpeg не может подключиться к RTMP потоку.

**Решение:**
1. Убедитесь, что RTMP сервер запущен
2. Проверьте, что Egress успешно стримит
3. Проверьте логи RTMP сервера

