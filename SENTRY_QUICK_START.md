# 🚀 Быстрый старт: Настройка Sentry за 5 минут

## Шаг 1: Создай проект в Sentry

1. Иди на [sentry.io](https://sentry.io) и зарегистрируйся
2. Создай новый проект → выбери **Next.js**
3. Скопируй **DSN** (он будет показан после создания проекта)

## Шаг 2: Добавь переменные окружения

### В `.env.local` (для локальной разработки):

```env
NEXT_PUBLIC_SENTRY_DSN=https://xxxxx@xxxxx.ingest.sentry.io/xxxxx
SENTRY_ORG=your-org-slug
SENTRY_PROJECT=rooms
```

**Где найти:**
- `NEXT_PUBLIC_SENTRY_DSN` — показывается сразу после создания проекта
- `SENTRY_ORG` — slug организации (можно найти в URL: `sentry.io/organizations/your-org-slug/`)
- `SENTRY_PROJECT` — название проекта (обычно `rooms`)

### В Vercel (для production):

1. Settings → Environment Variables
2. Добавь те же 3 переменные
3. Выбери окружения (Production, Preview)
4. Сохрани и пересобери проект

## Шаг 3: Проверь работу

1. Запусти приложение: `npm run dev`
2. В консоли браузера выполни:
   ```javascript
   throw new Error('Test Sentry error')
   ```
3. Проверь Sentry Dashboard — ошибка должна появиться через несколько секунд

## ✅ Готово!

Теперь все ошибки автоматически отправляются в Sentry:
- Ошибки React компонентов (через ErrorBoundary)
- Необработанные ошибки JavaScript
- Ошибки в API routes (серверная часть)

📖 **Подробная инструкция:** [docs/SENTRY_SETUP.md](./docs/SENTRY_SETUP.md)



