# 🔧 Исправление конфликта портов (актуальная версия)

## Что было
Когда RTMP слушал на 1935, Railway иногда ставил `PORT=1935`, и HTTP/WebSocket сервер пытался занять тот же порт → `EADDRINUSE`.

## Что делаем сейчас

1. **RTMP порт жёстко задаём как 1936**  
   В Variables должно быть:
   ```env
   RTMP_PORT=1936
   RTMP_INTERNAL_PORT=1936
   ```

2. **HTTP/WebSocket всегда слушает `process.env.PORT`**  
   - Никаких `WS_PORT` и кастомных портов.
   - Railway сам выдаёт свободный порт и пробрасывает HTTPS → наш сервер.

3. **Public Networking = Default / Auto**  
   - Settings → Public Networking → выбрать `Default`.
   - Это включает полноценный WebSocket туннель.  

## Что будет в логах
```
[WS-SERVER] Port configuration: { RTMP_PORT: 1936, envPORT: 42817, finalPORT: 42817 }
[WS-SERVER] ✅ WebSocket server running on port 42817
[RTMPServer] ✅ RTMP server started on port 1936
```

## Если Railway снова выставит PORT=1936
- Это означает, что RTMP_PORT случайно совпал.
- Просто поставь `RTMP_PORT=1937` (или любой другой свободный порт) и обнови TCP proxy.

Главное правило: **никаких WS_PORT**. HTTP сервер всегда слушает `process.env.PORT`, RTMP — свой отдельный порт. Тогда конфликтов больше не будет.***
