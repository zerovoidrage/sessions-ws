# Следующие шаги после подключения к Railway

## ✅ Что уже сделано

1. ✅ Репозиторий `sessions-ws` создан на GitHub
2. ✅ Railway проект подключен к репозиторию
3. ✅ Переменные окружения добавлены в Vercel/локально
4. ✅ Prisma зависимости добавлены

## 🔍 Что нужно проверить

### 1. Railway сервер запущен

**Проверка:**
1. Зайдите в Railway → ваш проект → сервис `sessions-ws`
2. Откройте вкладку **"Deployments"**
3. Проверьте, что последний деплой успешен (зеленый статус)
4. Откройте **"Logs"** и проверьте:

**Ожидаемые логи:**
```
[WS-SERVER] WebSocket server listening on port 3001
[RTMPServer] ✅ RTMP server started on port 1935
[WS-SERVER] ✅ RTMP server started for Room Composite Egress
```

**Если есть ошибки:**
- Проверьте переменные окружения на Railway
- Убедитесь, что `DATABASE_URL` установлен
- Проверьте, что все зависимости установлены

### 2. TCP-прокси для RTMP порта 1935

**Проверка:**
1. Railway → ваш сервис → **"Settings"** → **"Networking"**
2. Убедитесь, что добавлен порт **1935** с типом **TCP**
3. Должен быть создан публичный URL (например, `tcp://your-app.up.railway.app:1935`)

**Если не добавлен:**
1. Нажмите **"Add Port"** или **"New Port"**
2. Порт: `1935`
3. Protocol: `TCP`
4. Type: `Public`
5. Сохраните

### 3. Переменные окружения на Railway

**Обязательные переменные:**

```env
# Порт WebSocket сервера
PORT=3001

# RTMP сервер
RTMP_PORT=1935
RTMP_HOST=your-app.up.railway.app  # ⚠️ Замените на ваш Railway домен!

# LiveKit
LIVEKIT_HTTP_URL=https://your-project.livekit.cloud
LIVEKIT_API_KEY=your_livekit_api_key
LIVEKIT_API_SECRET=your_livekit_api_secret

# Gladia
GLADIA_API_KEY=your_gladia_api_key

# База данных
DATABASE_URL=your_database_url  # Та же, что в основном проекте
```

**Где добавить:**
- Railway → ваш сервис → **"Variables"** (или **"Settings"** → **"Variables"**)

**Важно:**
- `RTMP_HOST` должен быть Railway доменом (из Networking → Public Domain)
- `DATABASE_URL` должен быть тем же, что в основном проекте (Vercel)

### 4. Health check

**Проверка:**
```bash
# Откройте в браузере или через curl
https://your-app.up.railway.app/health

# Должен вернуть:
{"status":"ok"}
```

**Если не работает:**
- Проверьте, что Railway сервер запущен
- Проверьте логи на ошибки
- Убедитесь, что порт 3001 открыт

## 🧪 Тестирование

### Тест 1: Health check

```bash
curl https://your-app.up.railway.app/health
```

**Ожидаемый результат:** `{"status":"ok"}`

### Тест 2: Создание сессии

1. Откройте ваше приложение (Vercel или локально)
2. Создайте новую сессию
3. Присоединитесь к сессии
4. Проверьте логи Railway:

**Ожидаемые логи:**
```
[ServerTranscriber] Starting transcription for session ...
[RoomCompositeTranscriber] Starting transcription for session ...
[RTMPServer] ✅ RTMP server started on port 1935
[EgressTranscriber] ✅ Room Composite Egress started
```

### Тест 3: Транскрипция

1. В сессии начните говорить
2. Проверьте, что транскрипты появляются в UI
3. Проверьте логи Railway:

**Ожидаемые логи:**
```
[RTMPIngest] ✅ Stream started: /live/SESSION_SLUG
[GladiaBridge] Transcript received: ...
```

### Тест 4: Завершение сессии

1. Завершите сессию
2. Проверьте логи Railway:

**Ожидаемые логи:**
```
[ServerTranscriber] Stopping transcription for session ...
[RoomCompositeTranscriber] Egress stopped
[RTMPIngest] ✅ Stopped for session
```

## 🐛 Troubleshooting

### Проблема: Railway сервер не запускается

**Проверьте:**
1. Логи Railway на ошибки
2. Все переменные окружения установлены
3. `DATABASE_URL` правильный
4. Зависимости установлены (`npm install` выполнен)

### Проблема: RTMP порт не работает

**Проверьте:**
1. TCP-прокси добавлен в Networking
2. `RTMP_PORT=1935` установлен
3. `RTMP_HOST` = Railway домен (без порта)

### Проблема: Транскрипция не запускается

**Проверьте:**
1. `WS_SERVER_URL` правильный в Vercel/локально
2. Railway сервер отвечает на `/health`
3. Логи Railway на ошибки запуска транскрипции
4. `LIVEKIT_HTTP_URL`, `LIVEKIT_API_KEY`, `LIVEKIT_API_SECRET` установлены

### Проблема: Транскрипты не приходят

**Проверьте:**
1. `GLADIA_API_KEY` установлен на Railway
2. FFmpeg установлен (для RTMP декодирования)
3. Логи Railway на ошибки Gladia
4. RTMP поток подключен (логи `[RTMPIngest]`)

## 📋 Чек-лист перед production

- [ ] Railway сервер запущен и отвечает на `/health`
- [ ] TCP-прокси для порта 1935 добавлен
- [ ] Все переменные окружения установлены на Railway
- [ ] `WS_SERVER_URL` и `NEXT_PUBLIC_WS_HOST` установлены в Vercel
- [ ] Health check работает
- [ ] Создание сессии работает
- [ ] Транскрипция запускается при присоединении
- [ ] Транскрипты появляются в UI
- [ ] Завершение сессии работает корректно

## 🚀 После успешного тестирования

1. **Мониторинг:**
   - Настройте алерты на Railway для ошибок
   - Отслеживайте использование ресурсов

2. **Оптимизация:**
   - Настройте автопаузу для неактивных сессий
   - Оптимизируйте использование Gladia (батчинг, кэширование)

3. **Документация:**
   - Обновите документацию с production URL
   - Добавьте инструкции для команды

## 📞 Если что-то не работает

1. Проверьте логи Railway
2. Проверьте логи Vercel (если используется)
3. Проверьте браузерную консоль на ошибки
4. Убедитесь, что все переменные окружения правильные
5. Проверьте, что Railway домен доступен из интернета

