# Чеклист переменных окружения

## 📋 Для Render (WebSocket сервер)

### Обязательные переменные:

1. ✅ **DATABASE_URL**
   - PostgreSQL connection string (та же, что в Vercel)
   - Пример: `postgresql://user:password@host:5432/db?sslmode=require`

2. ✅ **TRANSCRIPTION_JWT_SECRET**
   - Секретный ключ для верификации JWT токенов транскрипции
   - **Должен совпадать** с `TRANSCRIPTION_JWT_SECRET` в Vercel
   - Можно использовать тот же `NEXTAUTH_SECRET` или сгенерировать отдельный: `openssl rand -base64 32`

3. ✅ **GLADIA_API_KEY**
   - API ключ от Gladia для транскрипции
   - Пример: `aeb596f4-b70e-4d92-a3de-8084b24ebf90`

4. ✅ **PORT** (или **WS_PORT**)
   - Порт для WebSocket сервера
   - Значение: `10000`
   - Render автоматически устанавливает `PORT`, но можно использовать `WS_PORT` как fallback

5. ✅ **NODE_ENV**
   - Окружение приложения
   - Значение: `production`

### Опциональные переменные (для расширенной функциональности):

6. **LIVEKIT_API_KEY**
   - Если нужен доступ к LiveKit API на сервере
   - Пример: `APILED8W5B2vGjd`

7. **LIVEKIT_API_SECRET**
   - Секрет LiveKit API
   - Пример: `JKKrI04fCYpxGuyBASig1MSnupSe7a9hVowB1pE2Q5`

8. **LIVEKIT_URL**
   - URL LiveKit сервера
   - Пример: `wss://omni-pxx5e1ko.livekit.cloud`

---

## 📋 Для Vercel (Next.js приложение)

### Обязательные переменные:

1. ✅ **DATABASE_URL**
   - PostgreSQL connection string
   - Пример: `postgresql://user:password@host:5432/db?sslmode=require`

2. ✅ **NEXTAUTH_SECRET**
   - Секретный ключ для NextAuth.js
   - Генерация: `openssl rand -base64 32`
   - Пример: `krT14H/LKWvL4Q0WGgx1LMNgiYoMmTG2RoM0Qc5K0tk=`

3. ✅ **NEXTAUTH_URL**
   - URL вашего приложения (БЕЗ слеша в конце)
   - Пример: `https://www.4sessions.space`
   - ⚠️ **Критично важно для OAuth redirect!**

4. ✅ **GOOGLE_CLIENT_ID**
   - OAuth Client ID из Google Cloud Console

5. ✅ **GOOGLE_CLIENT_SECRET**
   - OAuth Client Secret из Google Cloud Console

6. ✅ **LIVEKIT_API_KEY**
   - API ключ от LiveKit
   - Пример: `APILED8W5B2vGjd`

7. ✅ **LIVEKIT_API_SECRET**
   - API секрет от LiveKit
   - Пример: `JKKrI04fCYpxGuyBASig1MSnupSe7a9hVowB1pE2Q5`

8. ✅ **NEXT_PUBLIC_LIVEKIT_URL**
   - URL LiveKit сервера (для клиентской части)
   - Пример: `wss://omni-pxx5e1ko.livekit.cloud`

9. ✅ **TRANSCRIPTION_JWT_SECRET**
   - Секретный ключ для генерации JWT токенов транскрипции
   - **Должен совпадать** с `TRANSCRIPTION_JWT_SECRET` в Render
   - Можно использовать тот же `NEXTAUTH_SECRET` или отдельный
   - Пример: `krT14H/LKWvL4Q0WGgx1LMNgiYoMmTG2RoM0Qc5K0tk=`

10. ✅ **NEXT_PUBLIC_WS_HOST**
    - Хост WebSocket сервера транскрипции
    - ⚠️ **БЕЗ `https://`**, только доменное имя
    - Пример: `sessions-ws.onrender.com`
    - ❌ **Неправильно:** `https://sessions-ws.onrender.com`

11. ✅ **NEXT_PUBLIC_WS_PORT** (опционально)
    - Порт для dev-окружения
    - Значение: `10000` (или можно не указывать)
    - ⚠️ В production (HTTPS) код автоматически использует WSS на порту 443

12. ✅ **NODE_ENV**
    - Окружение приложения
    - Значение: `production`

### Опциональные переменные:

13. **GLADIA_API_KEY**
    - Если используется Gladia напрямую (обычно не нужно, так как используется через WebSocket сервер)
    - Пример: `aeb596f4-b70e-4d92-a3de-8084b24ebf90`

14. **OPENAI_API_KEY**
    - Если используется OpenAI для каких-либо функций

---

## ⚠️ Важные замечания

### 1. TRANSCRIPTION_JWT_SECRET должен совпадать

Эта переменная должна быть **одинаковой** в Vercel и Render, иначе верификация JWT токенов не будет работать.

### 2. NEXT_PUBLIC_WS_HOST без протокола

❌ **Неправильно:**
```
NEXT_PUBLIC_WS_HOST=https://sessions-ws.onrender.com
```

✅ **Правильно:**
```
NEXT_PUBLIC_WS_HOST=sessions-ws.onrender.com
```

Код автоматически определяет протокол (`wss://` для HTTPS, `ws://` для HTTP).

### 3. NEXTAUTH_URL критически важен для OAuth

Без правильного `NEXTAUTH_URL` Google OAuth redirect не будет работать, и пользователь будет возвращаться на страницу авторизации.

### 4. Проверка после деплоя

После настройки переменных:

1. ✅ Проверьте WebSocket сервер: `https://sessions-ws.onrender.com/health`
2. ✅ Проверьте OAuth redirect: войдите через Google
3. ✅ Проверьте транскрипцию: создайте сессию и проверьте работу

---

## 🔍 Быстрая проверка

### В Render:
- [ ] DATABASE_URL установлен
- [ ] TRANSCRIPTION_JWT_SECRET установлен
- [ ] GLADIA_API_KEY установлен
- [ ] PORT или WS_PORT установлен
- [ ] NODE_ENV = production

### В Vercel:
- [ ] DATABASE_URL установлен
- [ ] NEXTAUTH_SECRET установлен
- [ ] NEXTAUTH_URL установлен (БЕЗ слеша!)
- [ ] GOOGLE_CLIENT_ID и GOOGLE_CLIENT_SECRET установлены
- [ ] LIVEKIT_API_KEY и LIVEKIT_API_SECRET установлены
- [ ] NEXT_PUBLIC_LIVEKIT_URL установлен
- [ ] TRANSCRIPTION_JWT_SECRET установлен (совпадает с Render!)
- [ ] NEXT_PUBLIC_WS_HOST установлен (БЕЗ https://)
- [ ] NODE_ENV = production

---

## 🐛 Troubleshooting

### Проблема: WebSocket не подключается

**Проверьте:**
1. `NEXT_PUBLIC_WS_HOST` не содержит `https://`
2. WebSocket сервер работает: `https://sessions-ws.onrender.com/health`
3. `TRANSCRIPTION_JWT_SECRET` совпадает в Vercel и Render

### Проблема: OAuth redirect возвращает на страницу авторизации

**Проверьте:**
1. `NEXTAUTH_URL` установлен правильно (БЕЗ слеша)
2. `NEXTAUTH_SECRET` установлен
3. В Google Cloud Console правильно указан redirect URI: `https://your-domain.com/api/auth/callback/google`

### Проблема: Транскрипция не работает

**Проверьте:**
1. `TRANSCRIPTION_JWT_SECRET` совпадает в Vercel и Render
2. `GLADIA_API_KEY` установлен в Render
3. WebSocket сервер работает и отвечает на health check

