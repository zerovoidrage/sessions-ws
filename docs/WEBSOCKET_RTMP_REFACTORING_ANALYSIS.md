# Анализ рефакторинга WebSocket/RTMP сервера

## Обзор

Детальный анализ предложенных технических улучшений для WebSocket/RTMP сервера с оценкой рисков, совместимости и потенциальных проблем.

---

## Цель 1: Минимизировать задержку транскрипции

### 1.1 FFmpeg Low-Latency флаги

**Текущее состояние:**
```typescript
// server/rtmp-ingest.ts:196-213
const ffmpegArgs = [
  '-fflags', 'nobuffer',        // ✅ Уже есть
  '-flags', 'low_delay',         // ✅ Уже есть
  '-rtmp_live', 'live',          // ✅ Уже есть
  '-probesize', '4096',          // ⚠️ Предложено: '32'
  '-analyzeduration', '100000',  // ⚠️ Предложено: '0'
  // НЕТ: '-use_wallclock_as_timestamps', '1'
]
```

**Предложенные изменения:**
- `-probesize 32` (вместо 4096)
- `-analyzeduration 0` (вместо 100000)
- `-use_wallclock_as_timestamps 1` (новый флаг)

**Анализ рисков:**

✅ **Безопасно:**
- `-use_wallclock_as_timestamps 1` — безопасный флаг, улучшит синхронизацию

⚠️ **Риски:**
- `-probesize 32` — может быть слишком агрессивно для RTMP. RTMP требует минимального анализа потока для определения кодека. Значение 32 может привести к:
  - Ошибкам определения формата потока
  - Нестабильности при старте потока
  - **Рекомендация:** Использовать `-probesize 64` или `128` как компромисс

- `-analyzeduration 0` — может вызвать проблемы:
  - RTMP требует анализа для определения параметров потока
  - Может привести к ошибкам декодирования
  - **Рекомендация:** Использовать `-analyzeduration 50000` (50ms) как минимальное безопасное значение

**Вывод:** Частично безопасно. Нужны тесты с реальными RTMP потоками от LiveKit.

---

### 1.2 Размер PCM чанков

**Текущее состояние:**
```typescript
// server/rtmp-ingest.ts:240-247
const OPTIMAL_CHUNK_SIZE = 3200 // ~100ms аудио
let audioBuffer = Buffer.alloc(0)
let lastFlushTime = Date.now()
const FLUSH_INTERVAL_MS = 50 // Отправляем остатки каждые 50ms
```

**Анализ:**
- ✅ Уже реализовано оптимальное буферирование (~100ms)
- ✅ Есть flush interval (50ms) для остатков
- ✅ Размер чанка рассчитан правильно (3200 bytes = 100ms при 16kHz mono)

**Предложенные изменения:**
- Явная настройка размера чанка через env переменную
- Улучшение логики буферизации

**Риски:** Минимальные. Текущая реализация уже оптимальна.

**Рекомендация:** Добавить env переменную `PCM_CHUNK_SIZE_MS` для настройки, но оставить дефолт 100ms.

---

### 1.3 Direct Broadcast режим

**Текущее состояние:**
```typescript
// server/rtmp-ingest.ts:29-32
const REALTIME_BROADCAST_MODE =
  process.env.REALTIME_BROADCAST_MODE?.toLowerCase() === 'http'
    ? 'http'
    : 'direct'  // ✅ Уже по умолчанию 'direct'
```

**Анализ:**
- ✅ Direct режим уже используется по умолчанию
- ✅ HTTP режим только для split services

**Предложенные изменения:**
- Форсировать direct режим в monolith

**Риски:** Нет. Текущая логика уже правильная.

**Рекомендация:** Добавить явную проверку `SERVER_MODE !== 'rtmp'` для гарантии direct режима.

---

## Цель 2: Укрепить WebSocket слой

### 2.1 Heartbeat / Ping-Pong

**Текущее состояние:**
```typescript
// server/ws-handlers.ts:120-133
const pingInterval = setInterval(() => {
  if (ws.readyState === WebSocket.OPEN) {
    try {
      ws.ping()
    } catch (error) {
      console.error('[WS] Failed to send ping:', error)
      clearInterval(pingInterval)
    }
  } else {
    clearInterval(pingInterval)
  }
}, 30000) // Ping каждые 30 секунд
```

**Анализ:**
- ✅ Ping уже реализован (каждые 30 секунд)
- ❌ Нет обработки pong ответов
- ❌ Нет таймаута для неотвечающих клиентов
- ❌ Нет очистки клиентов при отсутствии pong

**Предложенные изменения:**
- Отслеживание pong ответов
- Таймаут для неотвечающих клиентов (закрытие соединения)
- Очистка из `sessionClients` при таймауте

**Риски:**

⚠️ **Средний риск:**
- Если клиент не поддерживает pong (старые браузеры, прокси), соединение будет закрыто
- Нужна обратная совместимость

**Рекомендация:**
- Добавить счетчик missed pongs (например, 3 пропущенных = закрытие)
- Логировать причину закрытия для диагностики
- Сделать интервал настраиваемым через env

---

### 2.2 Обработка ошибок в broadcast

**Текущее состояние:**
```typescript
// server/client-connection.ts:104-116
for (const clientMeta of clients) {
  if (clientMeta.ws.readyState === WebSocket.OPEN) {
    try {
      clientMeta.ws.send(message)
      sentCount++
    } catch (error) {
      console.error('[WS-SERVER] Failed to send transcript to client:', {
        sessionSlug,
        userId: clientMeta.userId,
        error,
      })
      // ❌ НЕТ: закрытия соединения и удаления из sessionClients
    }
  }
}
```

**Анализ:**
- ❌ Ошибки логируются, но клиент не удаляется
- ❌ Мертвые клиенты остаются в `sessionClients`
- ❌ Может накапливаться мусор в памяти

**Предложенные изменения:**
- Закрывать соединение при ошибке
- Удалять клиента из `sessionClients`

**Риски:**

✅ **Безопасно:**
- Улучшит стабильность системы
- Предотвратит утечки памяти
- Не сломает функциональность

**Рекомендация:** Реализовать немедленно. Это критический баг.

---

### 2.3 Backpressure (очереди)

**Текущее состояние:**
- ❌ Нет очередей для транскриптов
- ❌ Нет защиты от переполнения
- ❌ Broadcast блокирует event loop при большом количестве клиентов

**Предложенные изменения:**
- Per-session очереди
- Асинхронная обработка через `setImmediate`
- Дроп старых сообщений при переполнении (>1000)

**Риски:**

⚠️ **Средний риск:**
- Дроп сообщений может привести к потере транскриптов
- Нужна стратегия: что дропать (старые partial или старые final?)
- Может скрыть реальные проблемы (медленные клиенты)

**Рекомендация:**
- Реализовать очередь, но с предупреждениями при приближении к лимиту
- Логировать дропы для мониторинга
- Рассмотреть приоритизацию: final > partial
- Добавить метрику `ws.queue_drops_total` для алертов

---

### 2.4 Детальные метрики

**Текущее состояние:**
```typescript
// server/realtime-metrics.ts
// Есть: recordLatency(), recordCounter()
// Есть: getLatencySnapshot(), getCountersSnapshot()
```

**Анализ:**
- ✅ Базовая система метрик есть
- ❌ Нет per-session метрик
- ❌ Нет метрик для WebSocket слоя

**Предложенные изменения:**
- `ws.session_clients{sessionSlug}`
- `ws.session_broadcast_lag_ms{sessionSlug}`
- `ws.broadcast_errors_total`
- `ws.heartbeat_timeouts_total`

**Риски:** Минимальные. Только добавление метрик.

**Рекомендация:** Реализовать. Это улучшит мониторинг.

---

## Цель 3: Управление жизненным циклом

### 3.1 Очистка ресурсов при donePublish

**Текущее состояние:**
```typescript
// server/rtmp-ingest.ts:103-120
onStreamEnd: (streamPath: string) => {
  this.stopFFmpegDecoder()
  if (this.gladiaBridge) {
    this.gladiaBridge.close()
    this.gladiaBridge = null
  }
  this.stopAudioMetrics()
}
```

**Анализ:**
- ✅ Базовая очистка есть
- ⚠️ Нет проверки на "зомби" процессы
- ⚠️ Нет таймаута для kill процесса

**Предложенные изменения:**
- Проверка, что FFmpeg действительно остановлен
- Таймаут для SIGKILL
- Очистка всех ссылок из мап

**Риски:**

✅ **Безопасно:**
- Улучшит стабильность
- Предотвратит утечки ресурсов

**Рекомендация:** Реализовать. Это важно для production.

---

### 3.2 Защита от зомби-процессов

**Текущее состояние:**
```typescript
// server/rtmp-ingest.ts:365-433
this.ffmpegProcess.on('exit', (code, signal) => {
  // Есть обработка exit
  // Есть ретраи
})
```

**Анализ:**
- ✅ Есть обработка exit
- ✅ Есть ретраи (MAX_FFMPEG_RESTARTS = 3)
- ⚠️ Нет явной защиты от зависших процессов

**Предложенные изменения:**
- Таймаут для kill процесса
- Проверка, что процесс действительно завершился
- Логирование причин

**Риски:** Минимальные. Улучшит надежность.

**Рекомендация:** Реализовать.

---

### 3.3 Gladia Bridge обработка ошибок

**Текущее состояние:**
- Нужно проверить обработку ошибок в `gladia-bridge.ts`

**Риски:** Зависит от текущей реализации.

**Рекомендация:** Проверить и усилить обработку ошибок.

---

## Цель 4: Семантика транскриптов (дедупликация, partial/final)

### 4.1 Дедупликация по utteranceId

**Текущее состояние:**
```typescript
// Клиентская обработка (useRealtimeTranscript.ts:94)
if (data.type !== 'transcript' || data.sessionSlug !== sessionSlug) {
  return
}
// Клиент обрабатывает все сообщения с type: 'transcript'
```

**Анализ:**
- ❌ Нет дедупликации на сервере
- ✅ Клиент обрабатывает все сообщения
- ⚠️ Могут приходить дубликаты (например, при реконнекте)

**Предложенные изменения:**
- Per-session Set для последних N utteranceId
- Не отправлять дубликаты final транскриптов

**Риски:**

⚠️ **Высокий риск:**
- Если изменить логику отправки, клиенты могут не получить важные обновления
- Клиенты могут ожидать все сообщения для синхронизации состояния
- Может сломать логику на клиенте (например, `useTranscriptStream` группирует по utteranceId)

**Рекомендация:**
- ⚠️ **НЕ реализовывать дедупликацию на сервере** без изменения клиентов
- Вместо этого: добавить дедупликацию на клиенте (уже частично есть в `useTranscriptStream`)
- Или: добавить опциональный флаг `deduplicate: true` в сообщении для обратной совместимости

---

### 4.2 Разделение partial/final типов

**Текущее состояние:**
```typescript
// server/client-connection.ts:74-84
const payload: ServerTranscriptionMessage = {
  type: 'transcript',  // ❌ Всегда 'transcript'
  sessionSlug,
  text,
  isFinal: Boolean(isFinal),  // ✅ Есть флаг isFinal
  // ...
}
```

**Клиентская обработка:**
```typescript
// useRealtimeTranscript.ts:94
if (data.type !== 'transcript') return  // Ожидает 'transcript'

// useTranscriptStream.ts:98
if (json?.type === 'transcript') {  // Ожидает 'transcript'
  // Обрабатывает isFinal внутри
}
```

**Предложенные изменения:**
- `type: 'transcript_partial'` для partial
- `type: 'transcript_final'` для final

**Риски:**

🔴 **КРИТИЧЕСКИЙ РИСК:**
- **Сломает всех клиентов**, которые ожидают `type: 'transcript'`
- Нужно обновить:
  - `useRealtimeTranscript.ts`
  - `useTranscriptStream.ts`
  - `useLocalParticipantTranscription.ts`
- Нарушает требование "не менять публичные API"

**Рекомендация:**
- ❌ **НЕ реализовывать** без синхронного обновления всех клиентов
- Альтернатива: добавить новый тип, но поддерживать старый для обратной совместимости:
  ```typescript
  type: 'transcript',  // Старый формат (для совместимости)
  transcriptType: 'partial' | 'final',  // Новое поле
  ```
- Или: сделать это опциональным через feature flag

---

## Цель 5: Realtime AI Engine

### 5.1 Централизация AI вызовов

**Текущее состояние:**
```typescript
// src/hooks/useSessionAiEngine.ts
// AI вызовы происходят на клиенте
// Каждый клиент может вызывать AI независимо
```

**Анализ:**
- ❌ Нет координации между клиентами
- ❌ Несколько клиентов могут вызывать AI одновременно
- ❌ Дублирование запросов к OpenAI

**Предложенные изменения:**
- Перенести AI engine на сервер
- Или: координация через одного host клиента
- Блокировка per session

**Риски:**

⚠️ **Средний риск:**
- Если перенести на сервер:
  - Нужно добавить зависимость от БД в WebSocket сервер
  - Нужно добавить зависимость от OpenAI в WebSocket сервер
  - Увеличит сложность сервера
  - Нарушает разделение ответственности (AI логика должна быть в Next.js)

- Если оставить на клиенте с блокировкой:
  - Нужна координация через LiveKit data channel
  - Или через БД флаг (race conditions)
  - Или через WebSocket сервер (добавить координацию)

**Рекомендация:**
- **Вариант 1 (предпочтительно):** Оставить на клиенте, но добавить координацию через LiveKit data channel:
  - Один клиент (host) объявляет себя "AI coordinator"
  - Другие клиенты не вызывают AI
  - При отключении host — следующий клиент берет на себя роль

- **Вариант 2:** Добавить простую блокировку в БД:
  - Таблица `SessionAiLock` с `sessionId`, `lockedBy`, `lockedUntil`
  - Клиент проверяет блокировку перед вызовом
  - Устанавливает блокировку на 5 секунд
  - Автоматический release через TTL

---

## Общая оценка рисков

### ✅ Безопасные изменения (можно делать сразу)

1. **FFmpeg флаги** (частично):
   - ✅ `-use_wallclock_as_timestamps 1` — безопасно
   - ⚠️ `-probesize` и `-analyzeduration` — нужны тесты

2. **Обработка ошибок в broadcast** — критически важно, безопасно

3. **Очистка ресурсов** — безопасно, улучшит стабильность

4. **Метрики** — безопасно, только добавление

5. **Heartbeat улучшения** — безопасно с правильной реализацией

### ⚠️ Изменения с рисками (нужна осторожность)

1. **FFmpeg агрессивные флаги** — может сломать RTMP декодирование
2. **Backpressure с дропом** — может привести к потере транскриптов
3. **AI централизация** — увеличит сложность, но улучшит эффективность

### 🔴 Критические риски (НЕ делать без синхронного обновления клиентов)

1. **Разделение partial/final типов** — сломает всех клиентов
2. **Дедупликация на сервере** — может сломать логику клиентов

---

## Рекомендации по приоритизации

### Приоритет 1 (Критично, безопасно)

1. ✅ **Обработка ошибок в broadcast** — исправляет баг с утечками памяти
2. ✅ **Очистка ресурсов** — предотвращает зомби-процессы
3. ✅ **Heartbeat улучшения** — улучшит надежность соединений

### Приоритет 2 (Важно, нужны тесты)

4. ⚠️ **FFmpeg low-latency флаги** — улучшит задержку, но нужны тесты
5. ⚠️ **Backpressure** — улучшит производительность, но нужна стратегия дропа
6. ⚠️ **Метрики** — улучшит мониторинг

### Приоритет 3 (Нужна координация с фронтом)

7. 🔴 **Разделение partial/final** — НЕ делать без обновления клиентов
8. 🔴 **Дедупликация на сервере** — НЕ делать без обновления клиентов
9. ⚠️ **AI централизация** — нужен дизайн координации

---

## Потенциальные проблемы

### 1. Совместимость с клиентами

**Проблема:** Изменение формата сообщений (`type: 'transcript_partial'`) сломает всех клиентов.

**Решение:**
- Поддерживать оба формата (старый и новый) с feature flag
- Или: синхронное обновление всех клиентов

### 2. Потеря транскриптов при backpressure

**Проблема:** Дроп сообщений может привести к потере важных транскриптов.

**Решение:**
- Приоритизировать final > partial
- Логировать все дропы
- Алерты при превышении лимита

### 3. FFmpeg нестабильность

**Проблема:** Агрессивные low-latency флаги могут сломать декодирование.

**Решение:**
- Тестировать с реальными RTMP потоками
- Использовать более консервативные значения
- Добавить fallback на старые флаги при ошибках

### 4. AI координация

**Проблема:** Несколько клиентов вызывают AI одновременно.

**Решение:**
- Координация через LiveKit data channel (предпочтительно)
- Или: блокировка в БД с TTL

---

## Выводы

### Что можно делать безопасно:

1. ✅ Улучшение обработки ошибок в broadcast
2. ✅ Очистка ресурсов
3. ✅ Улучшение heartbeat
4. ✅ Добавление метрик
5. ✅ Частичное улучшение FFmpeg флагов (с тестами)

### Что нужно делать осторожно:

1. ⚠️ Агрессивные FFmpeg флаги (нужны тесты)
2. ⚠️ Backpressure с дропом (нужна стратегия)
3. ⚠️ AI координация (нужен дизайн)

### Что НЕ делать без синхронного обновления клиентов:

1. 🔴 Изменение формата сообщений (partial/final типы)
2. 🔴 Дедупликация на сервере (может сломать клиентов)

### Общая оценка:

**Риск сломать что-то:** Средний-Высокий (если делать все изменения)

**Рекомендация:** Делать изменения поэтапно:
1. Сначала безопасные (приоритет 1)
2. Потом с тестами (приоритет 2)
3. Последними — с координацией фронта (приоритет 3)

**Время на реализацию:** 2-3 дня для безопасных изменений, 1-2 недели для всех с тестами.

