# Анализ реализации LiveKit Egress для транскрипции

## ✅ Что сделано правильно

### 1. Выбор Track Egress
- ✅ Правильно выбран **Track Egress** для real-time транскрипции
- ✅ Использование WebSocket выхода соответствует документации
- ✅ Подход "один трек = один Egress" даёт полный контроль

### 2. Архитектура
- ✅ Корректная обработка входящих WebSocket соединений от Egress
- ✅ Правильная регистрация соединений в транскрайбере
- ✅ Микширование аудио потоков на сервере

### 3. Интеграция
- ✅ Правильное подключение к комнате для получения списка треков
- ✅ Обработка новых треков через RoomEvent.TrackSubscribed
- ✅ Публикация транскриптов через LiveKit data channel

## ⚠️ Критические проблемы

### 1. Проблема с лимитами Egress

**Проблема:**
- По умолчанию в LiveKit Cloud: **Concurrent Egress requests: Limit 2**
- При 5 участниках создаётся **5 Egress сессий** (по одной на трек)
- Это **превышает лимит в 2.5 раза** → Egress будет отклонять запросы

**Текущий код:**
```typescript
// Для каждого трека создаётся отдельная Egress сессия
for (const { trackId, participantIdentity } of audioTracks) {
  await this.startTrackEgressForTrack(trackId, participantIdentity)
  // При 5 участниках = 5 Egress сессий, но лимит только 2!
}
```

**Решения:**

#### Вариант A: Использовать Participant Egress (рекомендуется)
- **1 Egress сессия на участника** вместо 1 на трек
- При 5 участниках = 5 сессий (всё ещё превышает лимит, но лучше)
- Participant Egress экспортирует аудио+видео участника вместе
- Можно использовать только аудио часть

#### Вариант B: Использовать Room Composite Egress с audioOnly
- **1 Egress сессия на всю комнату** (микширование на стороне Egress)
- Проблема: Room Composite не поддерживает WebSocket выход напрямую
- Нужен RTMP или файловый вывод → не real-time

#### Вариант C: Батчинг Egress сессий
- Запускать максимум 2 Egress сессии одновременно
- Остальные треки обрабатывать через fallback (старый подход)

### 2. Формат аудио данных

**Проблема:**
- Track Egress отправляет аудио в **формате трека** (обычно **Opus**)
- Gladia требует **PCM16 16kHz моно**
- Текущий код **не декодирует Opus** → Gladia не сможет обработать

**Текущий код:**
```typescript
private handleEgressAudioData(trackId: string, data: WebSocket.Data): void {
  const buffer = Buffer.isBuffer(data) ? data : Buffer.from(data as ArrayBuffer)
  // ❌ Нет декодирования Opus → PCM16
  this.audioBuffers.get(trackId)!.push(buffer)
}
```

**Решение:**
- Добавить декодирование Opus до PCM16 16kHz
- Использовать библиотеку типа `@discordjs/opus` или `node-opus`
- Или использовать Track Egress с настройкой формата выхода (если поддерживается)

### 3. Обработка ошибок при превышении лимитов

**Проблема:**
- Нет обработки ошибок при превышении лимитов Egress
- При ошибке `startTrackEgress` код продолжает работать, но без транскрипции

**Текущий код:**
```typescript
try {
  const info = await this.egressClient.startTrackEgress(...)
} catch (error) {
  console.error(`Failed to start Track Egress for track ${trackId}:`, error)
  // ❌ Не прерываем процесс - продолжаем с другими треками
  // Но транскрипция для этого трека не работает!
}
```

**Решение:**
- Добавить fallback механизм
- При превышении лимитов использовать альтернативный подход
- Логировать предупреждения и метрики

## 🔧 Рекомендуемые улучшения

### 1. Использовать Participant Egress вместо Track Egress

**Преимущества:**
- Меньше Egress сессий (1 на участника вместо 1 на трек)
- Проще управление (участник = одна сессия)
- Автоматическое микширование аудио+видео участника

**Код:**
```typescript
// Вместо Track Egress для каждого трека
// Используем Participant Egress для каждого участника
for (const participant of room.remoteParticipants) {
  if (participant.audioTrackPublications.size > 0) {
    await this.startParticipantEgress(participant.identity)
  }
}
```

### 2. Добавить декодирование Opus

**Библиотека:**
```bash
npm install @discordjs/opus
# или
npm install node-opus
```

**Код:**
```typescript
import { OpusEncoder } from '@discordjs/opus'

private opusDecoder = new OpusEncoder(16000, 1) // 16kHz, mono

private handleEgressAudioData(trackId: string, data: WebSocket.Data): void {
  const opusBuffer = Buffer.isBuffer(data) ? data : Buffer.from(data as ArrayBuffer)
  
  // Декодируем Opus → PCM16
  const pcmBuffer = this.opusDecoder.decode(opusBuffer)
  
  this.audioBuffers.get(trackId)!.push(pcmBuffer)
}
```

### 3. Добавить обработку лимитов Egress

**Код:**
```typescript
private async startTrackEgressForTrack(trackId: string, participantIdentity: string): Promise<void> {
  try {
    const info = await this.egressClient.startTrackEgress(...)
    this.egressIds.push(info.egressId)
  } catch (error) {
    if (error.message.includes('limit') || error.message.includes('quota')) {
      console.warn(`[EgressTranscriber] Egress limit reached, using fallback for track ${trackId}`)
      // Использовать fallback подход (старый метод через livekit-client)
      await this.fallbackToClientTranscription(trackId)
    } else {
      throw error
    }
  }
}
```

### 4. Использовать Auto Egress (опционально)

**Преимущества:**
- Автоматический запуск Egress при подключении участников
- Не нужно вручную отслеживать новые треки
- Упрощает код

**Настройка:**
```typescript
// В настройках комнаты
await roomService.updateRoomMetadata(roomName, {
  autoEgress: {
    enabled: true,
    audioOnly: true,
    websocketUrl: `wss://your-server.com/egress/audio/{roomName}/{participantIdentity}`,
  },
})
```

## 📊 Сравнение подходов

| Подход | Egress сессий | Real-time | Микширование | Сложность |
|--------|---------------|-----------|--------------|-----------|
| **Track Egress** (текущий) | N (по трекам) | ✅ Да | На сервере | Средняя |
| **Participant Egress** | N (по участникам) | ✅ Да | На сервере | Средняя |
| **Room Composite** | 1 | ❌ Нет (RTMP) | На Egress | Низкая |
| **Auto Egress** | N (авто) | ✅ Да | На сервере | Низкая |

## 🎯 План действий

### Приоритет 1 (Критично)
1. ✅ **Добавить декодирование Opus** → PCM16 для Gladia
2. ✅ **Обработать лимиты Egress** → fallback механизм
3. ✅ **Рассмотреть Participant Egress** → меньше сессий

### Приоритет 2 (Важно)
4. ✅ **Добавить мониторинг** → метрики Egress сессий
5. ✅ **Улучшить обработку ошибок** → graceful degradation
6. ✅ **Добавить тесты** → проверка лимитов

### Приоритет 3 (Желательно)
7. ✅ **Рассмотреть Auto Egress** → упрощение кода
8. ✅ **Оптимизировать микширование** → более эффективный алгоритм
9. ✅ **Добавить кэширование** → уменьшение нагрузки

## 📝 Выводы

**Текущая реализация:**
- ✅ Архитектурно правильная
- ✅ Использует правильный API (Track Egress)
- ⚠️ Имеет проблемы с лимитами (5 участников > лимит 2)
- ⚠️ Не декодирует Opus → PCM16
- ⚠️ Нет обработки ошибок при превышении лимитов

**Рекомендации:**
1. **Немедленно**: Добавить декодирование Opus
2. **Немедленно**: Добавить обработку лимитов Egress
3. **В ближайшее время**: Рассмотреть Participant Egress
4. **В будущем**: Оптимизировать и добавить мониторинг

