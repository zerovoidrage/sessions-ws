# 🔧 Исправление конфликта портов

## Проблема:
Railway детектирует RTMP порт 1936 и устанавливает `PORT=1936` для HTTP/WebSocket сервера. Затем RTMP сервер пытается запуститься на том же порту 1936 и получает ошибку `EADDRINUSE`.

## Решение:

### 1. Добавь в Railway Variables:
```
WS_PORT=8000
```

Это заставит HTTP/WebSocket сервер слушать на порту 8000, независимо от того, что Railway установил в `PORT`.

### 2. После добавления переменной:
Railway автоматически перезапустит сервис. В логах должно быть:

```
[WS-SERVER] Port configuration: { RTMP_PORT: 1936, WS_PORT: 8000, envPORT: 1936, finalPORT: 8000 }
[WS-SERVER] ✅ WebSocket server running on port 8000
[WS-SERVER] ✅ RTMP server started for Room Composite Egress
```

### 3. Проверь TCP Proxy:
Убедись, что TCP proxy настроен на порт **1936** (для RTMP), а не на 8000.

## Альтернативное решение:

Если не хочешь использовать WS_PORT, можно изменить RTMP порт на другой (например, 1937), чтобы Railway не детектировал его. Но лучше использовать WS_PORT=8000.

