# 🔄 Изменение RTMP порта с 1935 на 1936

## Что изменено:

- RTMP порт изменён с `1935` на `1936` во всех файлах
- Это решает конфликт с Railway, который автоматически детектирует порт 1935 для WebSocket

## Файлы изменены:

- ✅ `server/index.ts` - убрана проверка на конфликт портов
- ✅ `server/rtmp-server.ts` - дефолтный порт 1936
- ✅ `server/rtmp-ingest.ts` - fallback порт 1936
- ✅ `server/livekit-room-composite-transcriber.ts` - внутренний порт 1936
- ✅ `server/livekit-transcriber.ts` - fallback порт 1936
- ✅ `Dockerfile` - EXPOSE 1936

## Что нужно сделать в Railway:

### 1. Обновить Variables:

Изменить:
```
RTMP_PORT=1935
```

На:
```
RTMP_PORT=1936
```

### 2. Обновить TCP Proxy:

В Railway Settings → Networking:
- Удалить старый TCP proxy для порта 1935
- Создать новый TCP proxy:
  - **Application Port:** `1936`
  - Railway создаст новый внешний порт (например, 37447 → 1936)

### 3. Обновить RTMP_EXTERNAL_PORT:

После создания нового TCP proxy, обновить:
```
RTMP_EXTERNAL_PORT=<новый_внешний_порт>
```

## После изменений:

- HTTP/WebSocket будет слушать на порту 8000 (или другом, который установит Railway)
- RTMP будет слушать на порту 1936 внутри контейнера
- Не будет конфликта с Railway auto-detection


